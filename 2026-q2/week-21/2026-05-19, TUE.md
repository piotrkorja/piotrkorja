### pomodoro #4

#### plan
- make bruno collection working
- filter by seeded tags on the main page
#### done
-  make bruno collection working
### pomodoro #3

#### plan
- read through the grok composition
- take notes
- make bruno collection working
#### done
-  read through the grok composition
- take notes
#### side notes

- http://localhost:3000/session/new
- http://localhost:3000/session

Request Method

POST

Status Code

302 Found

Remote Address

[::1]:3000

Referrer Policy

strict-origin-when-cross-origin
- 
- 
- Pre-request Scripts / Post-response Scripts for dynamic data (e.g., extract ID and set as env var: bru.setEnvVar("entityId", res.getBody().id);).
- For end-to-end feel, use Bruno's collection runner after logging in.
- Add **Assertions** (Assert tab) and **Tests** scripts for validation, e.g.: 
```JavaScript
test("status 200", () => expect(res.getStatus()).to.eql(200));
test("has tags", () => expect(res.getBody().tags).to.be.an('array'));
```
- Tip: Create a folder in your collection called Auth with Login/Signup/Logout requests. Use Collection Runner or scripts to login before other tests.
- Add variables like:
    - baseUrl: http://localhost:3000 (or your dev server)
    - email: test@example.com
    - password: yourpassword
    - Any other common values (API keys, etc.).
	- Use them in requests as {{baseUrl}} or {{email}}. Switch environments easily for different setups.
- Prefer YAML format for better Git experience.
- environments: dev, test, stage
- collection name `shershni tagging`
- `npm install`
- `npm install -g @usebruno/cli`
- Both `bin/rails parallel:setup[8]` and `bin/rails parallel:spec[8]` are not working locally for me